<Window x:Class="TeklaModelInspector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:TeklaModelInspector.Converters"
        Title="Tekla Model Inspector"
        Height="520"
        Width="900"
        MinHeight="400"
        MinWidth="700"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:StringToVisibilityConverter
        x:Key="StringToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="16"
                Background="#F3F4F6"
                CornerRadius="6">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Stav připojení"
                               Foreground="#666666"/>

                    <TextBlock Text="{Binding ConnectionStatus}"
                               Margin="0,4,0,0"
                               FontSize="18"
                               FontWeight="SemiBold"/>
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Margin="40,0,20,0">

                    <TextBlock Text="Otevřený model"
                               Foreground="#666666"/>

                    <TextBlock Text="{Binding ModelName}"
                               Margin="0,4,0,0"
                               FontSize="18"
                               FontWeight="SemiBold"/>
                </StackPanel>

                <Button Grid.Column="2"
        Width="140"
        Height="38"
        Content="Načíst prvky"
        Command="{Binding LoadPartsCommand}"
        IsEnabled="{Binding IsNotBusy}"/>
            </Grid>
        </Border>

        <Border Grid.Row="1"
        Margin="0,12,0,0"
        Padding="12"
        Background="#FDECEC"
        BorderBrush="#E57373"
        BorderThickness="1"
        CornerRadius="4"
        Visibility="{Binding ErrorMessage,
            Converter={StaticResource StringToVisibilityConverter}}">

            <TextBlock Text="{Binding ErrorMessage}"
               Foreground="#B71C1C"/>
        </Border>

        <Grid Grid.Row="2"
      Margin="0,20,0,8">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock VerticalAlignment="Center"
               Text="Prvky modelu"
               FontSize="20"
               FontWeight="SemiBold"/>

            <StackPanel Grid.Column="1"
                Orientation="Horizontal">

                <TextBlock Margin="0,0,8,0"
                   VerticalAlignment="Center"
                   Text="Hledat:"/>

                <TextBox Width="220"
                 Padding="7,4"
                 Text="{Binding SearchText,
                     UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding PartsView}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"
                                    Binding="{Binding Id}"
                                    Width="90"/>

                <DataGridTextColumn Header="Název"
                                    Binding="{Binding Name}"
                                    Width="*"/>

                <DataGridTextColumn Header="Profil"
                                    Binding="{Binding Profile}"
                                    Width="130"/>

                <DataGridTextColumn Header="Materiál"
                                    Binding="{Binding Material}"
                                    Width="130"/>

                <DataGridTextColumn Header="Typ"
                                    Binding="{Binding ObjectType}"
                                    Width="160"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="4"
      Margin="0,10,0,0">

            <TextBlock Foreground="#666666"
               Text="{Binding Parts.Count, StringFormat=Počet načtených prvků: {0}}"/>

            <ProgressBar Width="130"
                 Height="5"
                 HorizontalAlignment="Right"
                 IsIndeterminate="True"
                 Visibility="{Binding IsBusy,
                     Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>
    </Grid>
</Window>